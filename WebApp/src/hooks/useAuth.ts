"use client";

// Clerk auth hook - stub until dashboard is built
// Will be replaced with full Clerk implementation when /dashboard is ready
export function useAuth() {
  return { user: null, owner: null, loading: false };
}
